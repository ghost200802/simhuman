# SimHuman 项目规范

纯 2D 单机文明演化模拟游戏：玩家经营一个社群，从原始生存一直发展到工业与太空阶段。

当前阶段只有设计文档，Cocos 工程尚未初始化。

## 文档规则

- [docs/用户需求.md](docs/用户需求.md) 是唯一的需求来源，属**强制内容**。只增补、不改写原意，不把设计推导反写进去。
- `docs/` 下其余文档都是**衍生方案**，可修改、可拆分、可重写。方案与需求冲突时改方案。
- 各文档独立可读：不引用其他文档的编号或段落，不建立同步、包含或交叉引用关系。
- 一次只改当前任务涉及的文档。需要横向比较时，另出一份一次性评审材料，不把比较关系固化下来。
- 新增或调整需求条目时按主题归纳，不按提出顺序；每条用一句完整的话表达，全部视为必须执行。

## 技术选型

- Cocos Creator 3.8，纯 2D，单机离线，不做联网与服务端。
- TypeScript。
- 数据驱动：数值、内容与规则参数全部走 JSON 配置表。核心代码零魔法数值；新内容靠加配置实现，而不是加分支。
- 系统拆成两层：模拟核心持有唯一权威状态并推进 tick，表现层只渲染、只派发命令。两层代码物理隔离。

## 模拟核心红线

模拟核心内禁止：

- import `cc` 或任何引擎、平台 API；
- 规则路径使用 `Math.random`、`Date.now`、任何 IO；
- 除读取配置外自行产生数值；
- 把渲染对象或回调传进核心；
- 在 `step` 内出现「if 玩家命令」类分支 —— 命令必须在入口折成效果，不带进 tick。

违反任意一条，会同时破坏可测试性与可替换性。

## 编码纪律

保留「日后整体换实现语言」的余地，但不为此做额外架构工作：

1. 状态是可序列化的纯数据（record/数组 + 整数 ID），不用闭包跨 tick 持状态。
2. 逻辑是纯函数，结果只依赖入参与已记录的 rng 序列，不读全局。
3. 按领域拆小文件，模块间只经显式字段与 ID 交互。
4. 确定性：同种子 + 同命令序列得到同结果，回放与存档以此为准。

性能原型处标 `// PERF`。

## 协作方式

- 目标清晰时直接推进最短可行路径；目标不清晰时先列出缺失的决策点，不自行假设。
- 汇报时区分三类内容：用户已提出的需求、当前方案、仍可调整的推导。不把建议说成用户的要求。
- 指出路径代价与更优选择时单独说明，不混进执行结果。
